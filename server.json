{
  "server": {
    "port": 3000,
    "host": "0.0.0.0",
    "environment": "production",
    "note": "Variáveis sensíveis devem ser configuradas na Vercel"
  },
  "database": {
    "type": "mysql",
    "note": "DATABASE_URL será lida de process.env.DATABASE_URL na Vercel"
  },
  "auth": {
    "note": "OAuth credentials são lidas de variáveis de ambiente"
  },
  "security": {
    "jwtSecret": "LIDO DE: process.env.JWT_SECRET",
    "corsOrigins": ["*"],
    "https": true
  },
  "discord": {
    "webhookUrl": "LIDO DE: process.env.DISCORD_WEBHOOK_URL"
  },
  "payment": {
    "pixKey": "LIDO DE: process.env.PIX_KEY"
  }
}
